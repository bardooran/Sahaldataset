# Reviewed data

This area is for human-reviewed Somali data and annotations.

Reviewed data may later include words, sentences, morphology, grammar, language labels, or other linguistic judgments. Each collection should document its purpose, provenance, review method, and license/usage status.

A review example can be used for development only if its dataset split permits that use. Do not silently move frozen evaluation answers into training.
